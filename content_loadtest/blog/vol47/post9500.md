# Post 9500

This is **paragraph one** of post 9500, with *italic text* and build forge pipeline pipeline build template graph pipeline render forge pipeline cache.

Here's some `inline code` and a [link](https://example.com/9500).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post9500() { return 9500; }
```

Closing paragraph for post 9500, wrapping things up.
