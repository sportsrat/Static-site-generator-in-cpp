# Post 380

This is **paragraph one** of post 380, with *italic text* and forge hash render build cache render cache render markdown content render static.

Here's some `inline code` and a [link](https://example.com/380).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post380() { return 380; }
```

Closing paragraph for post 380, wrapping things up.
