# Post 280

This is **paragraph one** of post 280, with *italic text* and build pipeline hash static build graph markdown template graph static pipeline build.

Here's some `inline code` and a [link](https://example.com/280).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post280() { return 280; }
```

Closing paragraph for post 280, wrapping things up.
