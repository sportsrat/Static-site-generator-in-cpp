# Post 300

This is **paragraph one** of post 300, with *italic text* and node pipeline static markdown static template node node render static cache cache.

Here's some `inline code` and a [link](https://example.com/300).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post300() { return 300; }
```

Closing paragraph for post 300, wrapping things up.
