# Post 80

This is **paragraph one** of post 80, with *italic text* and graph static forge template render markdown cache pipeline hash forge graph node.

Here's some `inline code` and a [link](https://example.com/80).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post80() { return 80; }
```

Closing paragraph for post 80, wrapping things up.
