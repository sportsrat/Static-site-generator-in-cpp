# Post 60

This is **paragraph one** of post 60, with *italic text* and node node forge hash render build build template render node cache graph.

Here's some `inline code` and a [link](https://example.com/60).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post60() { return 60; }
```

Closing paragraph for post 60, wrapping things up.
