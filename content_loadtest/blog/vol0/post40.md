# Post 40

This is **paragraph one** of post 40, with *italic text* and build template graph build cache forge content forge template render markdown node.

Here's some `inline code` and a [link](https://example.com/40).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post40() { return 40; }
```

Closing paragraph for post 40, wrapping things up.
