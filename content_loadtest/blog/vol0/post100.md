# Post 100

This is **paragraph one** of post 100, with *italic text* and content cache cache build cache render forge template cache template static build.

Here's some `inline code` and a [link](https://example.com/100).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post100() { return 100; }
```

Closing paragraph for post 100, wrapping things up.
