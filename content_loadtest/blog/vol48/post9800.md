# Post 9800

This is **paragraph one** of post 9800, with *italic text* and pipeline markdown markdown cache forge hash static cache build markdown graph static.

Here's some `inline code` and a [link](https://example.com/9800).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post9800() { return 9800; }
```

Closing paragraph for post 9800, wrapping things up.
