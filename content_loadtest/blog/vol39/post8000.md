# Post 8000

This is **paragraph one** of post 8000, with *italic text* and static markdown forge template graph build hash content build markdown node static.

Here's some `inline code` and a [link](https://example.com/8000).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8000() { return 8000; }
```

Closing paragraph for post 8000, wrapping things up.
