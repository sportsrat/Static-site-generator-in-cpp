# Post 7980

This is **paragraph one** of post 7980, with *italic text* and graph static graph build build build build node render template graph node.

Here's some `inline code` and a [link](https://example.com/7980).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post7980() { return 7980; }
```

Closing paragraph for post 7980, wrapping things up.
