# Post 9960

This is **paragraph one** of post 9960, with *italic text* and forge static forge pipeline static forge render static build pipeline template template.

Here's some `inline code` and a [link](https://example.com/9960).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post9960() { return 9960; }
```

Closing paragraph for post 9960, wrapping things up.
