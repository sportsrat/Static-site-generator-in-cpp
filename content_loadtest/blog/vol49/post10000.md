# Post 10000

This is **paragraph one** of post 10000, with *italic text* and graph node graph cache markdown content cache static pipeline markdown static render.

Here's some `inline code` and a [link](https://example.com/10000).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post10000() { return 10000; }
```

Closing paragraph for post 10000, wrapping things up.
