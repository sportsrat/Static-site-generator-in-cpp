# Post 720

This is **paragraph one** of post 720, with *italic text* and forge hash cache markdown graph pipeline render cache pipeline hash forge template.

Here's some `inline code` and a [link](https://example.com/720).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post720() { return 720; }
```

Closing paragraph for post 720, wrapping things up.
