# Post 8300

This is **paragraph one** of post 8300, with *italic text* and content render template node content forge markdown template template render template content.

Here's some `inline code` and a [link](https://example.com/8300).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8300() { return 8300; }
```

Closing paragraph for post 8300, wrapping things up.
