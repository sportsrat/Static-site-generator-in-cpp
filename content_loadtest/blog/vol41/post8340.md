# Post 8340

This is **paragraph one** of post 8340, with *italic text* and static hash hash static cache cache forge content forge build build cache.

Here's some `inline code` and a [link](https://example.com/8340).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8340() { return 8340; }
```

Closing paragraph for post 8340, wrapping things up.
