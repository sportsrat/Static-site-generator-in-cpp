# Post 8008

This is **paragraph one** of post 8008, with *italic text* and forge cache render static graph build cache render pipeline static markdown node.

Here's some `inline code` and a [link](https://example.com/8008).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

Closing paragraph for post 8008, wrapping things up.
