# Post 8080

This is **paragraph one** of post 8080, with *italic text* and content graph render node cache forge pipeline render cache static render forge.

Here's some `inline code` and a [link](https://example.com/8080).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8080() { return 8080; }
```

Closing paragraph for post 8080, wrapping things up.
