# Post 8004

This is **paragraph one** of post 8004, with *italic text* and static render render forge render node build build markdown build node content.

Here's some `inline code` and a [link](https://example.com/8004).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

Closing paragraph for post 8004, wrapping things up.
