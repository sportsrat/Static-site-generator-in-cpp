# Post 8140

This is **paragraph one** of post 8140, with *italic text* and static forge static render static cache pipeline forge content build forge node.

Here's some `inline code` and a [link](https://example.com/8140).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8140() { return 8140; }
```

Closing paragraph for post 8140, wrapping things up.
