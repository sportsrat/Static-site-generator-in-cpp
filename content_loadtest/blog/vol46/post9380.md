# Post 9380

This is **paragraph one** of post 9380, with *italic text* and hash node template render static markdown hash static build graph render forge.

Here's some `inline code` and a [link](https://example.com/9380).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post9380() { return 9380; }
```

Closing paragraph for post 9380, wrapping things up.
