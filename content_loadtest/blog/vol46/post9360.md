# Post 9360

This is **paragraph one** of post 9360, with *italic text* and cache markdown hash markdown pipeline cache pipeline content forge content cache markdown.

Here's some `inline code` and a [link](https://example.com/9360).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post9360() { return 9360; }
```

Closing paragraph for post 9360, wrapping things up.
