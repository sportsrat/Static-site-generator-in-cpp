# Post 4100

This is **paragraph one** of post 4100, with *italic text* and render markdown build render node hash static build template static render graph.

Here's some `inline code` and a [link](https://example.com/4100).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post4100() { return 4100; }
```

Closing paragraph for post 4100, wrapping things up.
