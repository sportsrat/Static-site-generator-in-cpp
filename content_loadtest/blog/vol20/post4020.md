# Post 4020

This is **paragraph one** of post 4020, with *italic text* and node hash forge build pipeline render graph graph static content markdown markdown.

Here's some `inline code` and a [link](https://example.com/4020).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post4020() { return 4020; }
```

Closing paragraph for post 4020, wrapping things up.
