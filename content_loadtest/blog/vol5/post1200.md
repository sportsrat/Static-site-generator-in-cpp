# Post 1200

This is **paragraph one** of post 1200, with *italic text* and pipeline content build build build forge markdown node render pipeline build cache.

Here's some `inline code` and a [link](https://example.com/1200).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post1200() { return 1200; }
```

Closing paragraph for post 1200, wrapping things up.
