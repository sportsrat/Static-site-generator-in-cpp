# Post 8980

This is **paragraph one** of post 8980, with *italic text* and cache render build template build graph cache build static content build build.

Here's some `inline code` and a [link](https://example.com/8980).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8980() { return 8980; }
```

Closing paragraph for post 8980, wrapping things up.
