# Post 8960

This is **paragraph one** of post 8960, with *italic text* and build cache hash forge static hash cache render forge build graph markdown.

Here's some `inline code` and a [link](https://example.com/8960).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8960() { return 8960; }
```

Closing paragraph for post 8960, wrapping things up.
