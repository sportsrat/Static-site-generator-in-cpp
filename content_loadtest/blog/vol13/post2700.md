# Post 2700

This is **paragraph one** of post 2700, with *italic text* and render template markdown node forge static hash build hash markdown graph graph.

Here's some `inline code` and a [link](https://example.com/2700).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post2700() { return 2700; }
```

Closing paragraph for post 2700, wrapping things up.
