# Post 6560

This is **paragraph one** of post 6560, with *italic text* and forge build static static graph build forge cache cache template build markdown.

Here's some `inline code` and a [link](https://example.com/6560).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post6560() { return 6560; }
```

Closing paragraph for post 6560, wrapping things up.
