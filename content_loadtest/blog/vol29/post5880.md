# Post 5880

This is **paragraph one** of post 5880, with *italic text* and render static cache template pipeline forge content cache pipeline hash pipeline content.

Here's some `inline code` and a [link](https://example.com/5880).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post5880() { return 5880; }
```

Closing paragraph for post 5880, wrapping things up.
