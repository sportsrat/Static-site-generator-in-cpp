# Post 960

This is **paragraph one** of post 960, with *italic text* and cache static markdown render render node pipeline pipeline content pipeline forge cache.

Here's some `inline code` and a [link](https://example.com/960).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post960() { return 960; }
```

Closing paragraph for post 960, wrapping things up.
