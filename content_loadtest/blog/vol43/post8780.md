# Post 8780

This is **paragraph one** of post 8780, with *italic text* and cache static node graph graph cache graph static build template template graph.

Here's some `inline code` and a [link](https://example.com/8780).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8780() { return 8780; }
```

Closing paragraph for post 8780, wrapping things up.
