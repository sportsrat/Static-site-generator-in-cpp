# Post 8760

This is **paragraph one** of post 8760, with *italic text* and template node hash node build static forge render build graph build content.

Here's some `inline code` and a [link](https://example.com/8760).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8760() { return 8760; }
```

Closing paragraph for post 8760, wrapping things up.
