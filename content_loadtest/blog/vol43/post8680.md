# Post 8680

This is **paragraph one** of post 8680, with *italic text* and static content cache pipeline markdown node cache render graph markdown pipeline static.

Here's some `inline code` and a [link](https://example.com/8680).

- First point
- Second point with **emphasis**
- Third point

> A blockquote for good measure.

```
function post8680() { return 8680; }
```

Closing paragraph for post 8680, wrapping things up.
